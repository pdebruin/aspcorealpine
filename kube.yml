apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: null
  generation: 1
  labels:
    app: aspcorealpine
  name: aspcorealpine-deployment
  selfLink: /apis/extensions/v1beta1/namespaces/default/deployments/aspcorealpine-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aspcorealpine
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: aspcorealpine
    spec:
      containers:
      - image: dtr.dckr.it/pdebruin/aspcorealpine
        imagePullPolicy: Always
        name: k8s-aspcorealpine
        ports:
        - containerPort: 80
          protocol: TCP
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      tolerations:
      - key: com.docker.ucp.manager
        operator: Exists
status: {}
